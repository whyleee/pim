using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace App.Models
{
    public class ProductSections
    {
        public const string Header = nameof(Header);
        public const string Content = nameof(Content);
        public const string Nutrition = nameof(Nutrition);
        public const string Labels = nameof(Labels);
        public const string Storage = nameof(Storage);
        public const string Media = nameof(Media);
        public const string Publishing = nameof(Publishing);
    }
}
